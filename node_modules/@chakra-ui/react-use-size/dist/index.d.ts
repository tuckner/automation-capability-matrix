declare type Size = {
    width: number;
    height: number;
};
declare function useSizes<T extends HTMLElement | null>({ getNodes, observeMutation, }: {
    getNodes: () => T[];
    observeMutation?: boolean;
}): (Size | undefined)[];
declare function useSize<T extends HTMLElement | null>(subject: T | React.RefObject<T>): Size | undefined;

export { useSize, useSizes };
